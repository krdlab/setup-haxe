name: CI

on: [push]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
      fail-fast: true
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          haxe-version: 3.4.7
      - run: haxe -version
      - run: haxelib install hxnodejs

      - name: Compile test code
        run: haxe -cp test-workflow -main Main -js test-workflow.js
